---
description:
  zh-CN: "完整示例"
  en-US: "Input Number inclues min,max,step etc"
---

```html
<nz-input-number
  [(ngModel)]="${1:num}" name="${1}" id="${1}"
  [nzMin]="${2:1}" [nzMax]="${3:100}" [nzStep]="${4:1}" ${5: [nzPlaceHolder]="'${6}'"}></nz-input-number>
```
