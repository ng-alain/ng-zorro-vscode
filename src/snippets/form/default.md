---
description:
  zh-CN: "表单，向下为表单项"
  en-US: "Form"
---

```html
<form nz-form [formGroup]="${1:groupName}" (ngSubmit)="_submitForm($event, ${1:groupName}.value)"${2: nzLayout="${3|horizontal,vertical,inline|}"}>
  $0
</form>
```
