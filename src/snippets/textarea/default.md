## zh-CN

多选文本框

```html
<textarea nz-input [(ngModel)]="${1:remark}" id="${1}" name="${1}" [nzAutosize]="${2|true,false,{ minRows: 2 <自行改为逗号> maxRows: 6 }|}"${3: placeholder="${4}"}></textarea>$0
```

## en-US

Textarea

```html
<textarea nz-input [(ngModel)]="${1:remark}" id="${1}" name="${1}" [nzAutosize]="${2|true,false,{ minRows: 2 <Change to comma by yourself> maxRows: 6 }|}"${3: placeholder="${4}"}></textarea>$0
```
