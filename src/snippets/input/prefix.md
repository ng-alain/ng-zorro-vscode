---
description:
  zh-CN: "带有前缀图标的 input"
  en-US: "Input includes prefix"
---

```html
<input nz-input [(ngModel)]="${1:name}" id="${1}" name="${1}" nzPrefix="anticon anticon-${2:user}"${3: placeholder="${4}"}>
```
