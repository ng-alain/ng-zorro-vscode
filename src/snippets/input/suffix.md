---
description:
  zh-CN: "带有后缀图标的 input"
  en-US: "Input includes suffix"
---

```html
<input nz-input [(ngModel)]="${1:name}" id="${1}" name="${1}" nzSuffix="anticon anticon-${2:user}"${3: placeholder="${4}"}>
```
